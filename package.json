{
  "name": "gamernation-site",
  "private": true,
  "version": "1.0.0",
  "description": "GamerNation Inc. corporate site — static, hand-built, Raspberry Pi target. Zero dependencies; this file exists only so Node treats build.js/src as ES modules. Never run npm install.",
  "type": "module",
  "scripts": {
    "build": "node build.js"
  }
}
